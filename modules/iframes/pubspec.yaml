name: iframes
description: A collection of media iframes used on defuncart.com and blog.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0
