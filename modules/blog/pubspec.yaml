name: blog
description: A multi-category blog built using Flutter.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=2.10.0 <3.0.0"
  flutter: ">=1.22.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  material_design_icons_flutter: ^4.0.5755

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
