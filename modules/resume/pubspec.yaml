name: resume
description: A resume built using Flutter which is downloadable as a PDF.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.2
  pdf: ^3.0.1
  printing: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0
